# Signals & Sentiment — 環境變數設定
# 複製此檔為 .env 並填入你的值。.env 已在 .gitignore 中，不會被 commit。

# ── InfluxDB ──────────────────────────────────────────────
INFLUXDB_TOKEN=ptt-dev-token
INFLUXDB_ORG=ptt-lab
INFLUXDB_BUCKET=ptt_sentiment
INFLUXDB_ADMIN_PASSWORD=ptt-sentiment-2026

# ── Reddit PRAW (可選) ────────────────────────────────────
# 到 https://www.reddit.com/prefs/apps 建立 "script" app 取得
# 不設定則自動 fallback 到 public JSON API (60 req/min)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=

# ── Grafana ───────────────────────────────────────────────
GF_SECURITY_ADMIN_PASSWORD=admin

# ── InfluxDB Retention ────────────────────────────────────
# 資料保留天數 (預設 90 天，避免硬碟爆掉)
INFLUXDB_RETENTION_DAYS=90
